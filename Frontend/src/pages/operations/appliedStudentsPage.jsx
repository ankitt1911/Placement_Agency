import AppliedStudents from "../../components/operations/appliedStudents/appliedStudents";

export default function AppliedStudentsPage({ openLinkOnly = false }) {
  return <AppliedStudents openLinkOnly={openLinkOnly} />;
}
